---
const about = await Astro.glob('@about/markdown/about.md');
---

<section class="prose prose-invert">
  <!-- <Photo/> -->
  {about.map((h) => 
    <h1 class="text-3xl">{h.frontmatter.title}</h1>
    <p class="" set:html={h.compiledContent()} />
  )}
</section>