---
import { Image } from "astro:assets";

interface Props {
  biasName: string;
  // 👆🏽 es únicamente para los view transitions pero es la única forma de que funcione. Creo que es porque como lo estaba haciendo con el slug, en una ruta dinámica puede que en ocasiones el slug fuera "essay/bias/{bias-slug}" y en otras puede que sea "{bias-slug}". Seguramente haya formas de mejorarlo con un ID pero por ahora se ha solucionado 02/11/24
  src: string;
  alt: string;
  width: number;
  height: number;
}

const { alt, height, src, width, biasName } = Astro.props;
---

<Image
  src={src}
  height={height}
  alt={alt}
  width={width}
  class="inline-block aspect-video w-full h-full overflow-hidden object-cover object-center rounded-lg"
  transition:name={`${biasName}-image`}
/>
